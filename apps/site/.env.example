# Copy to `.env.local` for local testing (gitignored).
# Leave unset to keep analytics off during `pnpm dev` / local builds.

# Google Analytics 4 Measurement ID (format: G-XXXXXXXXXX)
# NEXT_PUBLIC_GA_MEASUREMENT_ID=
